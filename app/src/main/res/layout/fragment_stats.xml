<?xml version="1.0" encoding="utf-8"?>
<!-- 统计页面：动态背景 + 玻璃卡片 + iOS极简排版 -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_page">

    <!-- 动态背景层 -->
    <com.example.studytimer.DynamicBackgroundView
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- 内容层 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- 页面标题 -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="统计"
            android:textSize="32sp"
            android:textColor="@color/text_primary"
            android:fontFamily="sans-serif-medium"
            android:letterSpacing="0.02"
            android:paddingHorizontal="24dp"
            android:paddingTop="24dp"
            android:paddingBottom="4dp" />

        <!-- 日/周/月分段 -->
        <com.google.android.material.button.MaterialButtonToggleGroup
            android:id="@+id/toggle_period"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_marginHorizontal="24dp"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="16dp"
            app:singleSelection="true"
            app:checkedButton="@+id/btn_day">

            <Button
                android:id="@+id/btn_day"
                style="?attr/materialButtonOutlinedStyle"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:text="每日"
                android:textSize="13sp" />

            <Button
                android:id="@+id/btn_week"
                style="?attr/materialButtonOutlinedStyle"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:text="每周"
                android:textSize="13sp" />

            <Button
                android:id="@+id/btn_month"
                style="?attr/materialButtonOutlinedStyle"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:text="每月"
                android:textSize="13sp" />

            <Button
                android:id="@+id/btn_year"
                style="?attr/materialButtonOutlinedStyle"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:text="年"
                android:textSize="13sp" />

        </com.google.android.material.button.MaterialButtonToggleGroup>

        <!-- 日期导航 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:padding="14dp"
            android:background="@drawable/card_glass"
            android:elevation="2dp"
            android:layout_marginHorizontal="24dp"
            android:layout_marginBottom="12dp">

            <TextView
                android:id="@+id/btn_prev"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:text="‹"
                android:textSize="20sp"
                android:textColor="@color/blue_primary"
                android:fontFamily="sans-serif-medium"
                android:gravity="center"
                android:clickable="true"
                android:focusable="true"
                android:background="@drawable/bg_circle_btn" />

            <TextView
                android:id="@+id/tv_date_label"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:gravity="center"
                android:text="2026-05-30"
                android:textSize="15sp"
                android:textColor="@color/text_primary"
                android:fontFamily="sans-serif-medium"
                android:clickable="true"
                android:focusable="true"
                android:background="?attr/selectableItemBackground" />

            <TextView
                android:id="@+id/btn_next"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:text="›"
                android:textSize="20sp"
                android:textColor="@color/blue_primary"
                android:fontFamily="sans-serif-medium"
                android:gravity="center"
                android:clickable="true"
                android:focusable="true"
                android:background="@drawable/bg_circle_btn" />

        </LinearLayout>

        <!-- 总时长 -->
        <TextView
            android:id="@+id/tv_total_duration"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:padding="18dp"
            android:layout_marginHorizontal="24dp"
            android:layout_marginBottom="8dp"
            android:background="@drawable/card_elevated"
            android:elevation="4dp"
            android:text="总计：0 分钟"
            android:textSize="16sp"
            android:textColor="@color/blue_primary"
            android:fontFamily="sans-serif-medium" />

        <!-- 滚动内容 -->
        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:paddingTop="8dp"
            android:overScrollMode="never">

            <LinearLayout
                android:id="@+id/layout_stats_content"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingHorizontal="24dp"
                android:paddingBottom="24dp">

                <!-- 扇形图 -->
                <LinearLayout
                    android:id="@+id/chart_pie_container"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:background="@drawable/card_glass"
                    android:elevation="2dp"
                    android:layout_marginBottom="12dp"
                    android:padding="12dp" />

                <!-- 折线图 -->
                <LinearLayout
                    android:id="@+id/chart_line_container"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:background="@drawable/card_glass"
                    android:elevation="2dp"
                    android:layout_marginBottom="12dp"
                    android:padding="12dp" />

                <!-- 热力图（年模式） -->
                <LinearLayout
                    android:id="@+id/chart_heatmap_container"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:background="@drawable/card_glass"
                    android:elevation="2dp"
                    android:layout_marginBottom="12dp"
                    android:padding="12dp"
                    android:visibility="gone" />

                <!-- 列表 -->
                <LinearLayout
                    android:id="@+id/layout_stats_list"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical" />

            </LinearLayout>

        </ScrollView>

    </LinearLayout>

</FrameLayout>
