<?xml version="1.0" encoding="utf-8"?>
<!-- 全屏倒计时：动态渐变背景 + 圆形进度环 + 玻璃按钮 -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_page">

    <!-- 动态渐变背景 -->
    <com.example.studytimer.DynamicBackgroundView
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- 自定义背景图片 -->
    <ImageView
        android:id="@+id/iv_background"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:visibility="gone" />

    <!-- 渐变遮罩层 -->
    <View
        android:id="@+id/v_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_overlay_gradient" />

    <!-- 内容层 -->
    <LinearLayout
        android:id="@+id/layout_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:paddingHorizontal="24dp"
        android:paddingTop="32dp"
        android:paddingBottom="24dp">

        <!-- 顶部弹性 -->
        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <!-- 圆形计时环 -->
        <com.example.studytimer.CircularTimerView
            android:id="@+id/circular_timer"
            android:layout_width="280dp"
            android:layout_height="280dp" />

        <!-- 底部弹性 -->
        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <!-- 按钮行（玻璃卡片包裹） -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:orientation="horizontal"
            android:gravity="center"
            android:background="@drawable/card_glass"
            android:elevation="2dp"
            android:padding="6dp">

            <Button
                android:id="@+id/btn_pause_resume"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:layout_marginEnd="6dp"
                android:text="暂停"
                android:textSize="15sp"
                android:textColor="@color/blue_primary"
                android:fontFamily="sans-serif-medium"
                android:background="@drawable/btn_glass_outline"
                android:stateListAnimator="@null"
                android:elevation="0dp" />

            <Button
                android:id="@+id/btn_end"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:text="提前结束"
                android:textSize="15sp"
                android:textColor="@color/error"
                android:fontFamily="sans-serif-medium"
                android:background="@drawable/btn_pill_danger"
                android:stateListAnimator="@null"
                android:elevation="0dp" />

        </LinearLayout>

        <!-- 底部格言 -->
        <TextView
            android:id="@+id/tv_motto"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="自律即自由"
            android:textSize="13sp"
            android:textColor="@color/text_tertiary"
            android:gravity="center"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="8dp" />

    </LinearLayout>

</FrameLayout>
